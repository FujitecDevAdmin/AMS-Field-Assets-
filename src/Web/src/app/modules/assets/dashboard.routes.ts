import type { Routes } from '@angular/router';

import { AssetDashboardPage } from './features/dashboard/asset-dashboard.page';

export const ASSET_DASHBOARD_ROUTES: Routes = [{ path: '', component: AssetDashboardPage }];
