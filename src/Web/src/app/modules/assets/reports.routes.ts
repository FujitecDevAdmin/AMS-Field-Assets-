import type { Routes } from '@angular/router';

import { AssetReportsPage } from './features/reports/asset-reports.page';

export const ASSET_REPORTS_ROUTES: Routes = [{ path: '', component: AssetReportsPage }];
